import React from 'react'
import Counter from './components/Counter'

const App = () => {
  return (
    <div>
      <Counter />
    </div>
  )
}

export default App

// If you are using React, you need both Redux (state management library) and React-Redux (to connect Redux with React).